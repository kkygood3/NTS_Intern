<?xml version="1.0" encoding="UTF-8"?>
<web-app>

	<display-name>Spring JavaConfig Sample</display-name>
	<context-param>
		<param-name>contextClass</param-name>
		<param-value>org.springframework.web.context.support.AnnotationConfigWebApplicationContext
		</param-value>
	</context-param>
	<context-param>
		<!-- ApplicationConfig는 class 이름과 같아야 함 -->
		<param-name>contextConfigLocation</param-name>
		<param-value>com.nts.reservation.config.ApplicationConfig
		</param-value>
	</context-param>
	<!-- Server가 시작될 때(context가 loading될 때) ContextLoaderListener를 실행시킴 -> 위에 context-param을 이용함 -->
	<listener>
		<listener-class>org.springframework.web.context.ContextLoaderListener
		</listener-class>
	</listener>

	
	<servlet>
		<!-- (DispatcherServlet)을 등록 -->
		<servlet-name>mvc</servlet-name>
		<servlet-class>org.springframework.web.servlet.DispatcherServlet
		</servlet-class>
		<init-param>
			<!-- (AnnotationConfigWebApplicationContext) 이런 것을 사용할 것이다. -->
			<param-name>contextClass</param-name>
			<param-value>org.springframework.web.context.support.AnnotationConfigWebApplicationContext
			</param-value>
		</init-param>
		<init-param>
			<!-- DispatcherServlet이 실행될 때 (WebMvcContextConfiguration)에 들어있는 내용을 참고해라 -->
			<param-name>contextConfigLocation</param-name>
			<param-value>com.nts.reservation.config.WebMvcContextConfiguration
			</param-value>
		</init-param>
		<load-on-startup>1</load-on-startup>
	</servlet>
	<servlet-mapping>
		<!-- mvc(위에 servlet-name과 동일한 것)가 모든 요청('/')을 다 받겠다. -->
		<servlet-name>mvc</servlet-name>
		<url-pattern>/</url-pattern>
	</servlet-mapping>

	<!-- 요청 전/후로 처리 (encoding, url pattern) -->
	<filter>
		<filter-name>encodingFilter</filter-name>
		<filter-class>org.springframework.web.filter.CharacterEncodingFilter
		</filter-class>
		<init-param>
			<param-name>encoding</param-name>
			<param-value>UTF-8</param-value>
		</init-param>
	</filter>
	<filter-mapping>
		<filter-name>encodingFilter</filter-name>
		<url-pattern>/*</url-pattern>
	</filter-mapping>
</web-app>