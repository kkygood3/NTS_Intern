<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http#{//mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nts.reservation.dao.download.DownloadDao">
	<select id="selectDownloadInfo" resultType="DownloadInfo">
		SELECT
			f.file_name, f.save_file_name, f.content_type
		FROM
			reservation_user_comment_image AS rci
		INNER JOIN
			file_info AS f ON rci.file_id = f.id
		WHERE
			rci.id = #{reservationInfoImageId}
	</select>
</mapper>