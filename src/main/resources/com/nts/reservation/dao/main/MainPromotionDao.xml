<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nts.reservation.dao.main.MainPromotionDao">
	<select id="selectPromotions" resultType="MainPromotion">
		SELECT 
			d.id AS display_info_id, 
			f.save_file_name AS product_image_url 
		FROM
			promotion AS pm 
		INNER JOIN
			display_info AS d ON pm.product_id = d.product_id 
		INNER JOIN
			product_image AS pi ON pm.product_id = pi.product_id AND pi.type = 'th' 
		INNER JOIN
			file_info AS f ON pi.file_id = f.id 
		GROUP BY
			pm.product_id 
		LIMIT
			#{pagingLimit}
	</select>
	<select id="selectCount" resultType="int">
		SELECT
			COUNT(d.id)
		FROM
			promotion AS pm 
		INNER JOIN
			display_info AS d ON pm.product_id = d.product_id
	</select>
</mapper>